<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>QR Code Generator</title>
  <style>
    body {
      font-family: sans-serif;
      text-align: center;
      padding-top: 50px;
    }
    img {
      border: 2px solid #333;
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <h1>Scan this QR Code</h1>
  <p>A new code appears every 10 seconds</p>
  <img id="qr-code" src="/qr/image" alt="QR Code" width="300" height="300" />

  <script>
    setInterval(() => {
      const qrImg = document.getElementById('qr-code');
      qrImg.src = `/qr/image?timestamp=${Date.now()}`; // This ensures it's not cached
    }, 10000);  // Refresh every 10s
  </script>

</body>
</html>